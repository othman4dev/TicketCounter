if (data.length > 0) {
    console.log("setSessionStorage condition met");
    el.classList.add('active');
    error.textContent = '';
    return x ** y;
} else {
    console.log("Condition not met");
}