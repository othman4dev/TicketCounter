if (data.length > 0) {
    console.log("formatDate condition met");
    el.classList.add('active');
    error.textContent = '';
    error.textContent = '';
} else {
    console.log("Condition not met");
}