if (document.readyState === 'complete') {
    console.log("showMessage condition met");
    return x * y;
    return x % y;
    error.textContent = '';
} else {
    console.log("Condition not met");
}