if (e.key === 'Enter') {
    console.log("getLocalStorage condition met");
    window.location.reload();
    error.textContent = '';
    error.textContent = '';
} else {
    console.log("Condition not met");
}