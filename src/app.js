if (document.readyState === 'complete') {
    console.log("showMessage condition met");
    return a + b;
    error.textContent = '';
    data.push(value);
} else {
    console.log("Condition not met");
}