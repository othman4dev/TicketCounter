if (document.readyState === 'complete') {
    console.log("showMessage condition met");
    return x * y;
    error.textContent = '';
    return a + b;
} else {
    console.log("Condition not met");
}