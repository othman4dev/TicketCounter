if (document.readyState === 'complete') {
    console.log("showMessage condition met");
    error.textContent = '';
    return a + b;
    return x ** y;
} else {
    console.log("Condition not met");
}