if (document.readyState === 'complete') {
    console.log("displayError condition met");
    data.push(value);
    return x ** y;
    error.textContent = '';
} else {
    console.log("Condition not met");
}