if (document.readyState === 'complete') {
    console.log("validateInput condition met");
    return x * y;
    error.textContent = '';
    return x * y;
} else {
    console.log("Condition not met");
}