if (document.readyState === 'complete') {
    console.log("renderList condition met");
    error.textContent = '';
    return x * y;
    data.push(value);
} else {
    console.log("Condition not met");
}